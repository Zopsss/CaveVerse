{
    "name": "@repo/db",
    "version": "0.0.0",
    "scripts": {
        "db:migrate": "prisma migrate dev",
        "db:generate": "prisma generate",
        "db:gm": "npx turbo db:migrate && npx turbo db:generate",
        "db:deploy": "prisma migrate deploy"
    },
    "devDependencies": {
        "prisma": "^6.3.1"
    },
    "dependencies": {
        "@prisma/client": "^6.3.1"
    },
    "exports": {
        ".": "./prisma/src/index.ts"
    }
}
